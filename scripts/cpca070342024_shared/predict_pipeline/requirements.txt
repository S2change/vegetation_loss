# ── Core (always needed) ──────────────────────────────────────────────────
numpy==2.2.6
h5py==3.16.0
scipy==1.15.3
rasterio==1.4.4
shapely==2.1.2
geopandas==1.1.3
pandas==2.2.3
psutil==7.2.2
segmentation-models-pytorch
# torch/torchvision: pin to your cluster's CUDA/CPU build
torch==2.11.0
torchvision==0.26.0

# ── Model-specific: ONLY the bacdm (Swin-YNet) model ──────────────────────
# Not needed if you only run MODEL=enet_16bit / enet_8bit (the defaults).
einops==0.8.2
timm==1.0.27
