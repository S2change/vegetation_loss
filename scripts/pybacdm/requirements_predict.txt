# requirements_predict.txt
#
# Minimal dependencies for running predict.py (inference only).
# Covers the full import chain:
#   predict.py → swin_ynet.py → YTYAttention.py
#              → data/dataset_swin_GZ.py
#              → AAA_Configs.py
#
# Install
# -------
# CPU-only:
#   pip install -r requirements_predict.txt
#
# GPU (CUDA 12.x) — install torch first, then the rest:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   pip install -r requirements_predict.txt --no-deps torch torchvision
#
# Note: torch and torchvision must be installed with matching CUDA versions.
# Visit https://pytorch.org/get-started/locally/ for the correct install command.

# ── Deep-learning core ─────────────────────────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0

# ── Model architecture (swin_ynet.py) ─────────────────────────────────────────
timm>=0.9.0          # Swin transformer building blocks (DropPath, trunc_normal_)
einops>=0.6.0        # Tensor rearrangement used in attention layers

# ── Scientific computing ───────────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.10.0        # morphological closing + connected-component labelling
                     # (predict.py post-processing: binary_closing, label)

# ── Image I/O (dataset_swin_GZ.py) ────────────────────────────────────────────
Pillow>=9.0.0        # PIL.Image used for label loading
rasterio>=1.3.0      # GeoTIFF reading for chip input images
